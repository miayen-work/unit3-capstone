# Translate a natural-language question into a SQL query via Claude
# (Bedrock), then run it through sql_validation/validator.py before
# executing against Redshift. Never execute unvalidated SQL.
#
# TODO: implement once Redshift schema exists (Day 3).
